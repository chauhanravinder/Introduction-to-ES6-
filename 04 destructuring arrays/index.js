let [firstName, middleName, lastName] = ['Ravinder', 'Singh', 'Chauhan'];

let fullName = `${firstName} ${middleName} ${lastName}`;

lastName = 'Bassi';
let fullName2 = `${firstName} ${middleName} ${lastName}`;

console.log(fullName);
console.log(fullName2);